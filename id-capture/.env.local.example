# No local env vars needed by default.
# The frontend connects to the backend via the API_BASE constant in src/lib/api.ts
# If you need to override the backend URL for local testing:
# NEXT_PUBLIC_API_BASE=http://localhost:8000
